@model List<FixMyTask_Core_Project.Models.UserReviewModel>
@{
    ViewData["IsWorker"] = false;
}
<h2>Reviews</h2>

@if (Model == null)
{
    <div class="alert alert-warning"> No Reviews</div>
}
else
{
    @* <table class="table table-bordered">
        <thead>
            <tr>
                <th>Customer Name</th>
                <th>Worker Name</th>
                <th>Review</th>
                <th>Review Date</th>
                <th>Rating</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var review in Model)
            {
                <tr>
                    <td>@review.User_Name</td>
                    <td>@review.WorkerName</td>
                    <td>@review.Review</td>
                    <td>@review.ReviewDate.ToShortDateString()</td>
                    <td>@review.Rating</td>
                </tr>
            }
        </tbody>
    </table> *@

    <table class="table table-striped table-bordered">
        <thead class="table-dark">
            <tr>
                <th>Customer Name</th>
                <th>Worker Name</th>
                <th>Review</th>
                <th>Review Date</th>
                <th>Rating</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var review in Model)
            {
                <tr>
                    <td>@review.User_Name</td>
                    <td>@review.WorkerName</td>
                    <td>@review.Review</td>
                    <td>@review.ReviewDate.ToShortDateString()</td>
                    <td>
                        @for (int i = 1; i <= 5; i++)
                        {
                            if (i <= review.Rating)
                            {
                                <span class="text-warning">&#9733;</span> 
                            }
                            else
                            {
                                <span class="text-muted">&#9734;</span> 
                            }
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <a href="@Url.Action("WorkerProfile", "User", new {workerId=ViewBag.WorkerId})" class="btn btn-success mt-3">Back</a>
}


